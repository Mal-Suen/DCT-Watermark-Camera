<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 基础权限 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    
    <!-- 存储权限 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    
    <!-- Android 13+ 媒体权限 -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    
    <!-- 相机权限 -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="q">

        <!-- MainActivity - 启动页 -->
        <activity
            android:name="xju.dctcamera.activity.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 照片预览 -->
        <activity
            android:name="xju.dctcamera.activity.PhotoPreviewActivity"
            android:exported="false"
            android:label="@string/photo_preview"
            android:parentActivityName=".activity.MainActivity" />

        <!-- 获取照片 -->
        <activity
            android:name="xju.dctcamera.activity.GetPhotosActivity"
            android:exported="false"
            android:label="@string/get_photo" />

        <!-- 列表视图 -->
        <activity
            android:name="xju.dctcamera.activity.ListViewActivity"
            android:exported="false" />

        <!-- 照片 DCT 提取 -->
        <activity
            android:name="xju.dctcamera.activity.PhotoDedctActivity"
            android:exported="false"
            android:label="@string/dct_extract" />

        <!-- 设置 -->
        <activity
            android:name="xju.dctcamera.activity.SettingActivity"
            android:exported="false"
            android:label="@string/settings" />

        <!-- ShareSDK 回调 -->
        <activity
            android:name="xju.dctcamera.wxapi.WXEntryActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            tools:replace="android:theme" />

        <activity
            android:name="xju.dctcamera.apshare.ShareEntryActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            tools:replace="android:theme" />

        <!-- ShareSDK -->
        <activity
            android:name="com.mob.tools.MobUIShell"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:windowSoftInputMode="stateHidden|adjustResize" />

    </application>

</manifest>
